using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DT_Reader : MonoBehaviour{
    public static DT_Reader Instance;

    //CSV형태의 데이터 테이블을 저장하기 위한 Dictionary 파일....?
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
